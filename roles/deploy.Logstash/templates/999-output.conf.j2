output {
  if [beat_type] {
    elasticsearch {
      hosts => ["elastic-ingest.service.{{ consul_dc_name }}.consul:{{ elastic_http_port }}"]
      index => "%{[beat_type]}-%{[beat][version]}-%{+YYYY.MM.dd}"
    }
  }
  else {
    elasticsearch {
      hosts => ["elastic-ingest.service.{{ consul_dc_name }}.consul:{{ elastic_http_port }}"]
      index => "logstash-%{+YYYY.MM.dd}"
    }
  }
}
