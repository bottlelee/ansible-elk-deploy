- hosts: logstash
  become: yes
  serial:
    - 1
    - 10%
  roles:
    - deploy.Logstash
    - deploy.Consul
    - deploy.Monit
  post_tasks:
    - block:
        - wait_for:
            port: '{{ logstash_beats_port }}'
            timeout: 120
      rescue:
        - service:
            name: logstash
            state: started
        - wait_for:
            port: '{{ logstash_beats_port }}'
            timeout: 120
